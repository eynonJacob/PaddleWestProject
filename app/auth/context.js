//AuthContext is used across the app so user details from login can be accessed and edited across all screens

import React from "react";

const AuthContext = React.createContext();

export default AuthContext;
